import tensorflow as tf
import csv
from keras.preprocessing.text import Tokenizer
from keras.utils import pad_sequences

label = []
data = []

#reads json file
dataset = 

#splits up the information in the json file and saves it in lists 
for item in dataset:
    label , data = dataset.split(',')

print (label)